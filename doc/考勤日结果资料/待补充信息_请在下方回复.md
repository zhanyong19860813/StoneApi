# 考勤日结果资料 - 待补充信息清单

请在本文件中逐项回复，格式为：`**回复：** 你的回答`

---

## 一、理论考勤日结果.xlsx

1. **理论考勤日结果.xlsx** 是否已放入 `考勤日结果资料` 文件夹？如已放入，请确认。
   - **回复：** 
   理论考勤日结果.xlsx
   路径  D:\code\StoneApi\doc\考勤日结果资料\理论考勤日结果.xlsx

2. 若 Excel 结构与 ReadMe 中表格不同，请补充 Excel 的列名（表头）和示例值，或说明差异。
   - **回复：** 
  ReadMe中我只放了   理论考勤日结果.xlsx 文件中的 一部分列，我先看下效果
   存储过程 先 按 ReadMe 中的列生成
---

## 二、字段归类

3. 各字段的**归类**（用于建表注释）。以下为建议，请确认或修改：
   - **人员信息**：员工编号、员工姓名、部门、岗位
   - **日期信息**：日期、星期几
   - **班次信息**：班次类型、是否应出勤、应上班时间、应下班时间、应出勤工时、是否弹性班
   - **打卡信息**：上班打卡时间、下班打卡时间、打卡次数、打卡方式
   - **异常/审批信息**：是否缺卡、是否有审批单、审批类型、审批状态
   - **回复：** 
     这个问题你是要问我什么？我给你的这些表不是要新建的，都是已经存在的，不太好改 字段名。是我提供的信息不够完整吗？
---

## 三、数据来源与映射（步骤 2.3 填写字段）

4. **员工姓名**：是否从 `t_base_employee.name` 取值？
   - **可以** 

5. **部门**：显示部门 ID 还是部门名称？部门名称来自哪张表、哪个字段？
   - **部门名 和ID 都要有最好， select id 部门id, name 部门名称 from t_base_department：  你可以去查看 v_t_base_employee.sql 文件** 

6. **岗位**：显示岗位 ID 还是岗位名称？岗位名称来自哪张表、哪个字段？
   - **v_t_base_employee.sql 这个文件里面有说明** 

7. **班次类型**：对应 `att_lst_BC_set_code` 的哪个字段？是否有“固定班/弹性班”等类型？
   - **回复：** 

8. **是否弹性班**：从哪张表、哪个字段取值？若无，如何判断？
   - **这个字段你先不用写 留空** 

9. **打卡次数**：是否统计当天 `att_lst_Cardrecord` 的刷卡记录条数？
   - **对：** 

10. **打卡方式**：从哪张表、哪个字段取值？（如人脸/指纹/刷卡等）
    - *默认 人脸：** 

11. **是否有审批单 / 审批类型 / 审批状态**：审批表名、关联字段及取值规则？
    - **  目前是这样的 这个我们没有流程的数据，都是 按记录 等于 加班表，请假表 里面的数据为流程审核完成的结果。但流程本身不在我们这个系统：** 

---

## 四、存储过程与现有逻辑关系

12. `att_pro_DayResult_Standard` 与现有 `att_pro_DayResult` 的关系：
    - A) 全新独立，与现有日结并行
    - B) 替代现有日结，作为新的标准版
    - **全新独立  现有日结并行，但是你如果需要可以参考att_pro_DayResult ：** 

13. 人员范围逻辑：是否沿用现有逻辑（`att_Func_GetPower`、`ufn_split_string`、`ufn_get_dept_children` 等），还是需要新的规则？
    - **可用现有逻辑：** 

---

## 五、其他

14. 上述未覆盖的、你认为需要补充或说明的内容：
    - **暂时没有：** 

---

## 六、复核后待确认（请补充回复）

15. **班次类型**：问题 7 未填写。`att_lst_BC_set_code` 中有 `code_name`（排班名称）。建议：`班次类型` = `att_lst_BC_set_code.code_name`。是否按此映射？若有其他字段或规则请说明。
   - **select code_name 班次名称 from att_lst_BC_set_code：先忽略 班次类型吧 ** 

16. **审批三字段的具体取值**：有请假/加班记录即视为有审批且已审核。请确认：
    - **是否有审批单**：当天有 `att_lst_Holiday` 或 `att_lst_OverTime` 记录则「是」，否则「否」？
    - **审批类型**：有请假填「请假」，有加班填「加班」，两者都有填「请假,加班」？或其他格式？
    - **审批状态**：有记录即填「已审核」？
   - **1.有请假/加班记录即视为有审批且已审核 2.审批类型，加班记录 写加班，请假记录写请假 依此类推** 

17. **部门名称**：`v_t_base_employee` 中 `long_name`=直属部门，`longdeptname`=部门全路径。展示用哪个？
   - **回复：用long_name** 

---

*回复完成后保存本文件，再告知继续即可。*
