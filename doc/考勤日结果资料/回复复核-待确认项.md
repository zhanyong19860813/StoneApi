# 回复复核 - 待确认项

根据您在 `待补充信息_请在下方回复.md` 中的回复，我已整理。**已明确的信息足够开工**，仅剩 **2 项** 需您简单确认，其余已按合理默认处理。

---

## 一、已明确、可直接使用的信息

| 项目 | 结论 |
|------|------|
| 理论考勤日结果.xlsx | 路径已确认，先按 ReadMe 列生成 |
| 员工姓名 | 从 `t_base_employee.name` |
| 部门 | 部门 ID + 部门名称，参考 `v_t_base_employee`（dept_id、long_name / longdeptname） |
| 岗位 | 岗位 ID + 岗位名称，参考 `v_t_base_employee`（dutyId、dutyName） |
| 是否弹性班 | 暂留空 |
| 打卡次数 | 统计当天 `att_lst_Cardrecord` 记录条数 |
| 打卡方式 | 默认「人脸」 |
| 审批相关 | 无流程系统，以请假表、加班表记录为审核结果 |
| 存储过程 | 全新独立，与现有日结并行，可参考 `att_pro_DayResult` |
| 人员范围 | 沿用现有逻辑 |

---

## 二、需您确认的 2 项（请直接回复）

### 1. 班次类型

问题 7 未填写。`att_lst_BC_set_code` 中有 `code_name`（排班名称）。

**建议**：`班次类型` = `att_lst_BC_set_code.code_name`（班次名称，如「标准班」「长白班」等）

**请确认**：是否采用上述映射？若有其他字段或规则，请说明。

**您的回复：**
```
（在此填写，或写「按建议」）
```

---

### 2. 审批三字段的具体取值规则

当前理解为：有请假/加班记录 = 有审批单，记录即视为审核完成。需确认具体取值：

| 字段 | 建议规则 | 请确认或修改 |
|------|----------|--------------|
| **是否有审批单** | 当天有 `att_lst_Holiday` 或 `att_lst_OverTime` 记录则「是」，否则「否」 | |
| **审批类型** | 有请假记录填「请假」，有加班记录填「加班」，两者都有填「请假,加班」或您指定格式 | |
| **审批状态** | 有记录即填「已审核」或「已完成」 | |

**您的回复：**
```
（在此填写，或写「按建议」）
```

---

## 三、关于「字段归类」的说明（无需再回复）

您问的是：*「我给你的这些表不是要新建的，都是已经存在的，不太好改字段名」*

**说明**：我们要建的是 **新表**（如 `att_lst_DayResult_Standard`），不是改现有表。该表结构按 ReadMe/Excel 设计，列名如「员工编号」「员工姓名」「部门」等。  
「归类」仅用于**建表时的列注释**（如 `员工编号 - 人员信息`），方便后续维护，不涉及修改现有表结构。

归类将按之前建议执行，您无需再填写。

---

## 四、部门/岗位数据来源（已从 v_t_base_employee 确认）

- **部门 ID**：`dept_id`（或 `t_base_department.id`）  
- **部门名称**：`long_name`（直属部门）或 `longdeptname`（部门全路径），您希望用哪一个？若无特别要求，暂用 `long_name`。  
- **岗位 ID**：`dutyId`  
- **岗位名称**：`dutyName`  

**若部门名称要用全路径**，请在此说明：
```
（若用 longdeptname 请写「用全路径」，否则可写「用 long_name」或留空）
```

---

*请仅对「二、需您确认的 2 项」及「四」中部门名称做简短回复，其余已按现有信息处理。*
